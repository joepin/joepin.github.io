(function() {
  /*

  TODO: implement a page scrolling feature (scroll one "page" at a time)

  let winHeight = window.height;
  let winWidth = window.width;
  let curPage = 0;

  function getViewportDimensions() {
    winHeight = window.height;
    winWidth = window.width;
  }

  function scrollEventListener() {
    // get current position of scroller on the page
    // determine the strength of the scroll
    // calculate the end position of the scroll:
    // if it's greater than a page height, scrollto the next page
    // if not, do nothing (maybe display a little gesture?)
  }

  */
})();
